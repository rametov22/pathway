from .base import *

DATABASES = {
    "default": {
        "ENGINE": "django.db.backends.postgresql",
        "NAME": "pathway_db",
        "USER": "pathway_user",
        "PASSWORD": "Rr25808773",
        "HOST": "localhost",
        "PORT": "5432",
    }
}
